/**
 * Common enum for search criteria types used throughout the application
 * Defines the date criteria to search by for withdrawals and other date-based searches
 */
export enum SearchCriteriaType {
  REQUESTED_DATE = "REQUESTED_DATE",
  PROCESSED_DATE = "PROCESSED_DATE",
  ACCOUNT_DATE = "ACCOUNT_DATE",
}

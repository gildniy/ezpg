/**
 * Common enum for deposit format types used in the admin module
 * Defines whether a deposit uses static or dynamic virtual accounts
 */
export enum DepositFormatType {
  STATIC = "STATIC",
  DYNAMIC = "DYNAMIC",
}

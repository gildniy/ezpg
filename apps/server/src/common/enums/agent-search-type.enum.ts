/**
 * Common enum for agent search types used in the admin module
 * Defines the type of search criteria when searching for agents
 */
export enum AgentSearchType {
  USERNAME = "USERNAME",
  ACCOUNT_HOLDER = "ACCOUNT_HOLDER",
  ACCOUNT_NUMBER = "ACCOUNT_NUMBER",
}

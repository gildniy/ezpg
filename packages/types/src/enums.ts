export enum LogLevel {
  INFO = "info",
  WARN = "warn",
  ERROR = "error",
  DEBUG = "debug",
} /**
 * EZPG Types
 * Common types used across the EZPG project
 */
// For API client types, import from @ezpg/api-client

export enum TimePeriod {
  DAILY = "DAILY",
  WEEKLY = "WEEKLY",
  MONTHLY = "MONTHLY",
  YEARLY = "YEARLY",
}

export enum SortOrder {
  ASC = "asc",
  DESC = "desc",
}
